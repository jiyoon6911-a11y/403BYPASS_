package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// 403: BYPASS Accessibility Color Palette - Geometric Balance Theme
val NeonBlue = Color(0xFF0066FF)       // Geometric Primary - Brand Royal Blue (#0066FF)
val NeonBlueDim = Color(0xFF0052CC)    // Primary Variant
val ElectricCyan = Color(0xFF00D1FF)   // Secondary Accent - Bright Cyan (#00D1FF)

// High Contrast Backgrounds for visual accessibility (with Geometric Balance light mode style)
val SlateDark = Color(0xFFF4F7FA)      // Geometric Canvas Base (#F4F7FA)
val SlateCard = Color(0xFFFFFFFF)      // Geometric Card Background - Pure White
val SlateCardHeader = Color(0xFF002244) // Geometric Highlight Navy (#002244)
val HighContrastBlack = Color(0xFF000000) // Pitch black for high-contrast accessibility

// Typography Palette
val TextPrimary = Color(0xFF0F172A)    // High contrast slate 900 for dark text
val TextSecondary = Color(0xFF475569)  // Medium contrast slate 600 for subtitles
val TextPrimaryHC = Color(0xFFFFFFFF)  // Maximum white for dark background containers

// Semantic colors
val SafeGreen = Color(0xFF10B981)      // Balanced Emerald green
val HazardRed = Color(0xFFFF3B30)      // Geometric accessibility red (#FF3B30)
val WarningYellow = Color(0xFFF59E0B)  // Balanced amber yellow
val SoftGray = Color(0xFFE2E8F0)       // Geometric balanced borders (slate-200) (was SoftGray)
val OverlayDark = Color(0x99000000)

